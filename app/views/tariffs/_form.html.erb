<%= form_for(@tariff) do |f| %>
  <% if @tariff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tariff.errors.count, "error") %> prohibited this tariff from being saved:</h2>

      <ul>
      <% @tariff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :supplier_id %><br>
    <%= f.collection_select :supplier_id, Supplier.all, :id, :name, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.collection_select :service_id, Service.all, :id, :name, :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
